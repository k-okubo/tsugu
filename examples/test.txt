def sum(n) {
  if (n < 1) {
    0;
  } else {
    n + sum(n - 1);
  };
}

def foo() {
  val sum10 = sum(10);

  if (sum10 > 0) {
    if (sum(5) > 0) {
      sum10;
    } else {
      1;
    };
  } else {
    0;
  };
}

def main() {
  foo();
}
