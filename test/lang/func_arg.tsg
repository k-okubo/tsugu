// RUN: cat %s | %tsugu | FileCheck %s
// CHECK: result = 66

def apply(f, x) {
  f(x);
}

def double(x) {
  2 * x;
}

def triple(x) {
  3 * x;
}

def main() {
  apply(double, 3) + apply(triple, 20);
}
